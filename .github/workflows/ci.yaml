name: CI
on:
  push:
    branches: ['*']

jobs:
  golangci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup_go@v6
        with:
          go-version-file: go.mod
      - run: make test
